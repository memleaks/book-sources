Due to the nuget restore issue, open ria-cs.sln and build whole solution.
If build error occured, reopen ria-cs.sln and build again should success.

My redis server is running in a LAN ubuntu server with ip 192.168.10.18,
you can change configuration of redis in helper.dll.config.
